on:
  schedule:
#    - cron: '0 23 * * *'
  workflow_dispatch:

name: drivers-stage3

jobs:
  docker:
    runs-on: arc-runner-eco4cast
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      OSN_KEY: ${{ secrets.OSN_KEY }}
      OSN_SECRET: ${{ secrets.OSN_SECRET }}
    container:
      image: eco4cast/rocker-neon4cast:latest
      options: --memory 45g
    steps:
      - uses: actions/checkout@v4

      - name: Download Pseudo
        shell: Rscript {0}
        run: |
          source("drivers/download_psuedo.R")

      - name: Update Stage3
        shell: Rscript {0}
        run: |
          source("drivers/update_stage3.R")

      - name: Healthcheck
        run: |
          curl -m 10 --retry 5 https://hc-ping.com/49d4676e-7379-4699-ad40-f5d58df0ad95
